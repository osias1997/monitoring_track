psutil>=5.9.0
colorama>=0.4.6
flask>=3.0.0
watchdog>=4.0.0
rich>=13.0.0
scapy>=2.5.0
plotly>=5.18.0

# Optional (enable in behavior_config.py):
# frida>=16.0.0
# frida-tools>=12.0.0
# pywintrace>=0.1.0

# ============================================================================
# PACKET CAPTURE: install Npcap from https://npcap.com/
#   (enable WinPcap API-compatible Mode). Run as Administrator.
#
# FRIDA: pip install frida frida-tools
# ETW: works via WinEvent polling by default; optional native pywintrace.
# DLL SIGNATURES / ETW: Administrator recommended.
